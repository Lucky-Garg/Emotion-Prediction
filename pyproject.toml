[project]
name = "emotion-prediction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lucky-Garg", email = "lucky.work.005@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "keras>=3.15.1",
    "numpy>=2.5.2",
    "pydantic>=2.13.4",
    "tensorflow>=2.21.0",
    "uvicorn[standard]>=0.52.3",
]

[project.scripts]
emotion-prediction = "emotion_prediction:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
